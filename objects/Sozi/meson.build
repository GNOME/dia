# TODO: This depends on WITH_SOZI compilation option.
if false
    sources = files(
        'sozi.c',
        'sozi-object.c',
        'sozi-frame.c',
        'sozi-media.c'
    )

    dia_object_desc += {
        'name': 'sozi_objects',
        'sources': sources,
    }
endif
